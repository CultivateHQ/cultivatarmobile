# DummyNerves

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add `dummy_nerves` to your list of dependencies in `mix.exs`:

    ```elixir
    def deps do
      [{:dummy_nerves, "~> 0.1.0"}]
    end
    ```

  2. Ensure `dummy_nerves` is started before your application:

    ```elixir
    def application do
      [applications: [:dummy_nerves]]
    end
    ```

